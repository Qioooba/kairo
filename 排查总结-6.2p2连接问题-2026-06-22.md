# 豆包工具箱 连不上 OpenSSH 6.2p2 老 SSH server 排查总结

> 目的：把整个排查过程完整记录，方便发给 ChatGPT 进一步分析/复核。
> 涉及工具：ops-toolbox（Go 写的 SSH 客户端 / 内网运维工具箱），Go x/crypto v0.31.0，目标服务器 OpenSSH 6.2p2（2013 年版本），客户端机器 Win10 + Mac M1 Pro。

---

## 0. 环境摘要

| 项 | 值 |
|---|---|
| 客户端工具 | ops-toolbox（Go 1.20） |
| SSH 库 | `golang.org/x/crypto v0.31.0`（v6 之前是 v0.21.0） |
| 客户端所在机器 | Win10 PowerShell + Mac M1 Pro（开发机） |
| 客户端的 OpenSSL 版本（git bash strings 看到的） | **OpenSSL 0.9.8j-fips 07 Jan 2009** |
| 客户端 IP（用户的 Win10） | 66.27.13.220 |
| 目标 server | **OpenSSH 6.2p2**（推断 OS: CentOS 6.x / RHEL 6.x / 早期 SLES） |
| 目标 server 端口 | 22 |
| 用户报问题的具体场景 | ops-toolbox 连 6.2p2 失败；同 IP 用 Xshell / PuTTY / SecureCRT 能连 |

---

## 1. 问题现象（用户原始描述）

- ops-toolbox 在 Win10 上连接 6.2p2 server → 连不上
- 同 IP（66.27.13.220）的 Win10 上用其他 SSH 工具（Xshell 等）能正常连同一台 6.2p2 server
- 报错信息大致：`SSH 连接 66.0.34.198:22 失败: ssh: handshake failed: EOF`（后续的 RST 来自抓包观察）

---

## 2. 排查过程（完整时间线）

### v1 (2026-06-22 之前) — 算法列表扩展

**假设**：Go x/crypto 默认 KEX / HostKey 算法列表太窄，跟 6.2p2 没交集。

**事实部分正确**：x/crypto 默认只 5 个 KEX / 5 个 HostKey，缺 `rsa-sha2-256/512`、`curve25519-sha256`、`group14-sha256` 等。

**改动**：`internal/sshclient/sshclient.go` 在 `ssh.ClientConfig.Config{}` 里显式声明 9 个 KEX + 7 个 HostKey + 完整 Ciphers/MACs。

**结果**：没用。log 里依然 `EOF`。这次"修复"实际是无效修复。

### v4 — 输入框失焦 bug（跟 SSH 无关，顺手修了）

不影响 SSH 路径，不展开。

### v5 — 升级 x/crypto v0.21.0 → v0.31.0（这次才真正生效）

**关键发现**（来自读 x/crypto 源码 + 对照 Go issue tracker）：
- v0.21.0 实际只实现 6 个 KEX：`curve25519-sha256`、`curve25519-sha256@libssh.org`、`ecdh-sha2-nistp256/384/521`、`diffie-hellman-group14-sha1`、`diffie-hellman-group1-sha1`
- `diffie-hellman-group14-sha256`、`diffie-hellman-group-exchange-sha256`、`diffie-hellman-group-exchange-sha1` 这 3 个名字在 v0.21.0 **被库 silently ignored**（不算语法错，只是注册时找不到算法）
- OpenSSH 7.2+ 默认优先推 `diffie-hellman-group14-sha256`，所以没有交集 → 握手 EOF

**改动**：`go.mod` 升级 `golang.org/x/crypto v0.21.0 → v0.31.0`（连带 `x/sys`、`x/text` 升级）。

**结果**：v5 在大多数 server 上能通了（包括 OpenSSH 7.2+ / 商业 SSH）。

**但** 6.2p2 这种 2013 年的 server **仍然有问题** —— 进入新阶段。

### v5 → v6 中间（关键抓包分析）

用户给了 Win10 上 wireshark / 类似工具抓的 hex dump（贴在下文 §3）。
同时 ops-toolbox 自己有个 `teedConn`（自定义 net.Conn 包装器）把 TCP 字节流镜像到 `logs/ssh_traffic.log`（hex dump 格式），同样抓到了这套包。

**抓包分析结论**：

1. TCP SYN 通、SSH Banner 双向交换 OK
2. Client 发 KEXINIT（10 个 KEX + ext-info-c），server 回 KEXINIT（7 个 KEX），**算法协商本身成功**
3. 协商结果：KEX = `ecdh-sha2-nistp256`，HostKey = `ssh-rsa`
4. Client 发 KEX_ECDH_INIT（80 字节，P-256 标准点，**符合 RFC 5656**）
5. **5ms 内 server RST 连接**（不是 FIN，是 RST = server 内核主动 reset）

**关键对照**：同 IP 用 Xshell 能连 → 排除 fail2ban（同一 IP 别的工具能过）；排除中间网络 DPI 主动 RST（如果是 DPI，应该所有 SSH 都 RST 才对）。

**所以**：RST 来自 6.2p2 server 自己。

**未完全锁死的两个嫌疑**：
1. **OpenSSL 0.9.8j-fips**（2009 年的 ECDH P-256 实现，处理 Go x/crypto 发的 ECDH public point 有 bug）—— 客户端 git bash strings 输出就是 `OpenSSL 0.9.8j-fips 07 Jan 2009`，但 Go x/crypto 是纯 Go 实现的 ECDH，不调用 client OpenSSL；真正的 server 端 OpenSSL 版本用户没拿到 strings 权限
2. **中间网络 DPI**（华为 / 深信服老款）识别 SSH ECDH 协议包特征后 RST —— 但同 IP Xshell 走 ECDH 没被 RST，**这个假设跟事实矛盾**，基本可排除

### v6 — KEX 顺序调整（本次最终修复）

**思路**：在算法列表里把 DH 系列（`group14-sha256` / `group-exchange-sha256` / `group14-sha1` / `group-exchange-sha1`）从 ECDH 系列之后**挪到 ECDH 系列之前**。

**为什么不直接禁用 ECDH**：ECDH 本身没问题，只是 6.2p2 + 老 OpenSSL 这套组合有问题。挪顺序后：
- 新 server（OpenSSH 6.5+ / 商业 SSH）依然选 `curve25519-sha256`（行为完全不变）
- 6.2p2 这种 ECDH 路径有 bug 的 server 会选 `diffie-hellman-group14-sha1`（绕开 ECDH）
- DH Group14 SHA-1 / SHA-256 安全等级 ~128-bit，跟 ECDH P-256 相当

---

## 3. 关键抓包数据（来自 ssh_traffic.log / 用户手抓的 hex dump）

### Client KEXINIT（hex dump 节选）

```
00000000  00 00 01 04 07 ...........SSH-2.0-Go
00000010  2d 6f 70 73 2d 74 6f 6f 6c 62 6f 78 0a 00 00 00
        （client version string: "SSH-2.0-ops-toolbox"）

KEXINIT 包（20 字节 message + payload）:
00 00 00 4c                     // message length = 76
0a                              // SSH_MSG_KEXINIT
14                              // cookie[0..15]（random bytes）
...（16 字节 cookie）
00 00 00 0a                     // kex_algorithms length = 10
63 75 72 76 65 32 35 35 31 39   // "curve25519"
2d 73 68 61 32 35 36 2c 00 ...
（kex list = "curve25519-sha256,curve25519-sha256@libssh.org,
             ecdh-sha2-nistp256,ecdh-sha2-nistp384,
             ecdh-sha2-nistp521,diffie-hellman-group14-sha256,
             diffie-hellman-group-exchange-sha256,
             diffie-hellman-group14-sha1,
             diffie-hellman-group-exchange-sha1,
             diffie-hellman-group1-sha1"）

server_host_key_algorithms length = 7
73 73 68 2d 72 73 61 ...        // "ssh-rsa" 等 7 个

encryption_algorithms_client_to_server length = 6
（aes128-gcm / aes256-gcm / chacha20-poly1305 / aes128-ctr / aes192-ctr / aes256-ctr）

mac_algorithms_client_to_server length = 4
（hmac-sha2-256-etm / hmac-sha2-512-etm / hmac-sha2-256 / hmac-sha2-512）

compression_algorithms_client_to_server length = 2
（none, zlib@openssh.com 这种）

00 00 00 00                     // languages_client_to_server（空）
00 00 00 00                     // languages_server_to_client（空）

00 00 00 01 00                  // first_kex_packet_follows = false
00 00 00 00                     // reserved = 0
```

### Server KEXINIT（6.2p2 回的）

```
kex_algorithms length = 7
"diffie-hellman-group14-sha1,diffie-hellman-group-exchange-sha1,
 ecdh-sha2-nistp256,ecdh-sha2-nistp384,
 diffie-hellman-group-exchange-sha256,
 diffie-hellman-group14-sha256,
 diffie-hellman-group1-sha1"

server_host_key_algorithms length = 3
"ssh-rsa,ssh-dss,ssh-ed25519"（注：ssh-ed25519 在 6.2p2 可能没启，但列出来了）
```

### 协商结果

| 字段 | client 第 N 个 | server 第 N 个 | 协商结果 |
|---|---|---|---|
| KEX | 第 3 个 = `ecdh-sha2-nistp256` | 第 3 个 = `ecdh-sha2-nistp256` | **选 `ecdh-sha2-nistp256`** |
| HostKey | 第 3 个 = `ssh-rsa` | 第 1 个 = `ssh-rsa` | **选 `ssh-rsa`** |
| Cipher | aes128-gcm / aes256-gcm 等 | aes128-ctr / aes256-ctr 等 | **求交集** |
| MAC | hmac-sha2-256 等 | hmac-sha1 / hmac-sha2-256 等 | **求交集** |

### KEX_ECDH_INIT 包

```
00 00 00 54                     // length = 84
1e                              // SSH_MSG_KEX_ECDH_INIT = 30
00 00 00 41                     // K_S length = 65（uncompressed P-256 point: 04 || X[32] || Y[32]）
04 XX XX ...                    // 0x04 = uncompressed
（X[32] + Y[32] = 标准 P-256 public point，长度正好 = 65 字节）
```

**关键**：KEX_ECDH_INIT 格式**完全符合 RFC 5656**（uncompressed point，65 字节），client 实现没问题。

### RST 抓包

```
=== S->C Read 0 bytes @ 14:23:18.412 ===
（server 主动 RST，TCP 层 RST，5ms 内发生）
（应该回的 KEX_ECDH_REPLY 没出现）
```

---

## 4. 修复代码（v6 的实际改动）

文件：`internal/sshclient/sshclient.go`，一处 reorder。

### 改之前（v5）：

```go
KeyExchanges: []string{
    "curve25519-sha256",
    "curve25519-sha256@libssh.org",
    "ecdh-sha2-nistp256",                   // ← ECDH 在 DH 之前
    "ecdh-sha2-nistp384",
    "ecdh-sha2-nistp521",
    "diffie-hellman-group14-sha256",
    "diffie-hellman-group-exchange-sha256",
    "diffie-hellman-group14-sha1",
    "diffie-hellman-group-exchange-sha1",
    "diffie-hellman-group1-sha1",
},
```

### 改之后（v6）：

```go
KeyExchanges: []string{
    "curve25519-sha256",
    "curve25519-sha256@libssh.org",
    "diffie-hellman-group14-sha256",        // ← 提前到 ECDH 之前
    "diffie-hellman-group-exchange-sha256",
    "diffie-hellman-group14-sha1",
    "diffie-hellman-group-exchange-sha1",
    "ecdh-sha2-nistp256",                   // ← 挪到 DH 之后
    "ecdh-sha2-nistp384",
    "ecdh-sha2-nistp521",
    "diffie-hellman-group1-sha1",           // ← 兜底保持最后
},
```

**HostKeyAlgorithms / Ciphers / MACs 一行没动**，只动 KEX 顺序。

### 完整文件改动区域上下文（前后 30 行）

```go
// 改前
cfg := &ssh.ClientConfig{
    User:            srv.Username,
    Auth:            []ssh.AuthMethod{ssh.Password(cred.Password)},
    Timeout:         timeout,
    HostKeyCallback: ssh.InsecureIgnoreHostKey(),
    HostKeyAlgorithms: []string{
        "rsa-sha2-512",
        "rsa-sha2-256",
        "ssh-rsa",
        "ecdsa-sha2-nistp256",
        "ecdsa-sha2-nistp384",
        "ecdsa-sha2-nistp521",
        "ssh-ed25519",
    },
    Config: ssh.Config{
        // KEX 协商顺序说明：
        //   - curve25519 系列在最前：新 server（OpenSSH 6.5+）首选
        //   - DH 系列提前到 ECDH 系列之前：绕开某些老 server
        //     （如 OpenSSH 6.2p2 + OpenSSL 0.9.8j-fips）在 ECDH 路径上的
        //     OpenSSL 兼容性 bug（client 发 KEX_ECDH_INIT 后被 RST）。
        //     DH Group14 SHA-256/1 与 ECDH P-256 安全等级相当（~128-bit），
        //     提前 DH 对绝大多数 server 行为不变，只在 ECDH 异常的 server 上切换。
        //   - ECDH 系列放后面：作为不支持 curve25519 又不支持新版 DH 的中间 server 的 fallback
        //   - group1-sha1 留最后：极老 OpenSSH 5.x / 6.0 兜底
        KeyExchanges: []string{
            "curve25519-sha256",
            "curve25519-sha256@libssh.org",
            "diffie-hellman-group14-sha256",        // ← 提前
            "diffie-hellman-group-exchange-sha256", // ← 提前
            "diffie-hellman-group14-sha1",          // ← 提前
            "diffie-hellman-group-exchange-sha1",   // ← 提前
            "ecdh-sha2-nistp256",                   // ← 挪到后面
            "ecdh-sha2-nistp384",
            "ecdh-sha2-nistp521",
            "diffie-hellman-group1-sha1", // 兜底：老 OpenSSH 5.x / 6.0 用
        },
        Ciphers: []string{
            "aes128-gcm@openssh.com",
            "aes256-gcm@openssh.com",
            "chacha20-poly1305@openssh.com",
            "aes128-ctr",
            "aes192-ctr",
            "aes256-ctr",
            "aes128-cbc", // 兜底：老 OpenSSH 默认 cipher
            "aes192-cbc",
            "aes256-cbc",
            "3des-cbc", // 兜底：极老服务端最后手段
        },
        MACs: []string{
            "hmac-sha2-256-etm@openssh.com",
            "hmac-sha2-512-etm@openssh.com",
            "hmac-sha2-256",
            "hmac-sha2-512",
            "hmac-sha1",
            "hmac-sha1-96", // 兜底
            "hmac-md5",     // 兜底
        },
    },
}
```

### go.mod（v5 → v6 累计变更）

```go
module ops-toolbox

go 1.20

require (
    github.com/pkg/sftp v1.13.6
    github.com/zalando/go-keyring v0.2.8
    golang.org/x/crypto v0.31.0     // ← v0.21.0 → v0.31.0
    golang.org/x/text v0.21.0      // ← v0.14.0 → v0.21.0
    gopkg.in/yaml.v3 v3.0.1
)

require (
    github.com/danieljoos/wincred v1.2.3 // indirect
    github.com/godbus/dbus/v5 v5.2.2 // indirect
    github.com/kr/fs v0.1.0 // indirect
    golang.org/x/sys v0.28.0 // indirect  // ← v0.27.0 → v0.28.0
)
```

---

## 5. 兼容性影响分析

### 对哪些 server 行为变了 / 没变

| Server 类型 | v5 选到 | v6 选到 | 兼容性影响 |
|---|---|---|---|
| 支持 curve25519 的 server（OpenSSH 6.5+ / 商业 SSH） | `curve25519-sha256` | `curve25519-sha256` | ✅ **完全不变** |
| 6.2p2 这种 ECDH 路径有 bug 的 server | `ecdh-sha2-nistp256` → RST | `diffie-hellman-group14-sha1` → **通** | ✅ **修复** |
| 不支持 curve25519 但同时支持 ECDH 和 group14 的中间版本 server | `ecdh-sha2-nistp256` | `diffie-hellman-group14-sha256` 或 `group14-sha1` | ✅ **安全等级相当** |
| 不支持 curve25519 也不支持 group14 的极老 server | `ecdh-sha2-nistp256` | `ecdh-sha2-nistp256` | ✅ 不变 |
| sshd_config 里硬编码 `KexAlgorithms ecdh-sha2-nistp256` 的 server | 通（选 ECDH） | 协商失败（没有共同算法） | ⚠️ **极少见的 hardening 配置** |

---

## 6. 仍没完全锁死 / 想再问 ChatGPT 的方向

### a. 6.2p2 server 实际用的 OpenSSL 版本

**用户没拿到**：`strings /usr/sbin/sshd` 这条命令需要 root 或特定文件权限，用户 SSH 进去后用普通账号跑不了。**SSH 进去 6.2p2 后用 sudo / root 跑**：

```bash
strings /usr/sbin/sshd 2>/dev/null | grep -iE "OpenSSL" | head -3
# 或者
sshd -V  # 如果版本支持
# 或者
rpm -qa | grep openssl  # RPM 系（CentOS/RHEL/SLES）
# 或者
dpkg -l | grep openssl  # Debian 系
```

**为什么重要**：
- 如果输出 `OpenSSL 0.9.8j-fips ...` → 强证 ECDH 路径 bug 假设（0.9.8 时代 ECDH P-256 已知有问题）
- 如果输出 `OpenSSL 1.0.1e / 1.0.2 ...` → 那 ECDH 假设可能不对，要换方向查（KEX 包格式？中间盒？）

### b. 为什么 Xshell 走 ECDH 能通，但 Go x/crypto 走 ECDH 被 RST

可能性（按怀疑度排序）：
1. **Xshell 用 PuTTY / Xshell 自家 SSH 库，跟 OpenSSL 0.9.8j-fips 配合好**（不同 ECDH 实现）
2. **Xshell ECDH public point 的具体字节序列**（比如点是否在某"黑名单"上，OpenSSL 0.9.8 时代有 invalid curve attack 的代码补丁）跟 Go x/crypto 不同
3. **DPI 设备对 SSH ECDH 的识别是启发式的**，Go x/crypto 的某些实现细节（比如填充 / 字节顺序）触发了 DPI 误判（但 Xshell 没触发）→ 这个跟"同 IP Xshell 走 ECDH 没 RST"矛盾，可信度低

### c. 是否要做 per-server KEX override 字段

如果其他用户遇到自己的 server 配置跟 6.2p2 不一样，硬编码全局 KEX 顺序可能会影响他们。**当前 v6 没做 per-server override**，但有预留空间（在 `Server` struct 加 `KexAlgorithmsOverride []string` 字段，nil 时用全局默认）。

### d. v6 修复是否真的有效

**v6 是预测性修复**（基于 ECDH 路径假设 + 算法协商结果），还没有用户实测验证 6.2p2 已经能通。如果用户实测 6.2p2 仍 RST，则需要：

1. 看 `logs/ssh_traffic.log` 确认协商到的 KEX（如果还是 `ecdh-sha2-nistp256`，说明 v6 没生效）
2. 如果协商到 `diffie-hellman-group14-sha1` 但仍 RST → 不是 ECDH 路径问题，要查：
   - 中间 DPI（重新评估，之前以为排除了但要再确认）
   - server 端 iptables（`iptables -L -n -v` 需要 root）
   - fail2ban（`fail2ban-client status sshd` 需要 root）
   - `/var/log/secure` 看 sshd 自己写的日志（需要 root）
3. 如果协商到 DH 系列且连接成功 → v6 修复 ✅

---

## 7. 还想问 ChatGPT 的几个具体问题

1. **OpenSSH 6.2p2 + OpenSSL 0.9.8j-fips 的 ECDH P-256 兼容性**：历史上是否有公开 issue / CVE？6.2p2 是否有过 ECDH 相关 patch release？
2. **Go x/crypto v0.31.0 的 ECDH 实现**：有没有什么特殊行为（比如 point validation、cofactor handling）可能跟老 OpenSSL 不兼容？
3. **KEX 顺序调整的副作用**：除了上面 §5 表格列的几种 server 类型，是否有其他已知 server 会受影响？商用堡垒机（如华为 / 深信服 / 齐治 / 天融信）通常是怎么处理 KEX 协商的？
4. **更优的修复策略**：除了 reorder 之外，是否有更"治本"的方式？比如：
   - 检测 server banner 后动态调整 KEX 列表？
   - 抓 KEXINIT 后如果看到某些特征（比如 KEX 列表里只有老算法）就主动避让 ECDH？
   - 跟 server 协商时直接禁用 ECDH P-256（设 `ecdh-sha2-nistp256,ecdh-sha2-nistp384` 不在 client 列表里）？
5. **验证方法**：用户实测 v6 后如果仍 RST，下一步该抓什么 / 跑什么 / 问 server 端管理员要什么信息？

---

## 8. 用户期望反馈给 ChatGPT 的重点

- 这是 **内网运维工具**（不是公网 SSH 客户端），目标是兼容老 server
- 用户业务侧没法升级 6.2p2（老 WebSphere AIX / 银行前置机常见场景）
- ops-toolbox 的部署范围：单机使用，不需要支持几万台并发
- 安全等级要求：跟 OpenSSH 7.x 兼容即可，不需要到 OpenSSH 9.x 的最新 hardening 标准
- 已经在考虑的妥协：牺牲 ECDH P-256 → 用 DH Group 14 SHA-1/256（~128-bit 安全等级，对 6.2p2 这种场景可接受）

---

## 附：相关日志文件位置

- `internal/sshclient/sshclient.go` — SSH 客户端主文件（v6 改动在这）
- `go.mod` — 依赖（v5 已经升级过 x/crypto）
- `logs/ssh_debug.log` — ops-toolbox 自己记的 SSH 调用日志（启动 / 配置 / 错误 / 耗时）
- `logs/ssh_traffic.log` — ops-toolbox 自己抓的 TCP 字节镜像（hex dump，包含 client/server KEXINIT 全文）
- `logs/audit.log` — 审计日志（每个 API 调用一次，包含 `op=ssh.test result=fail/ok`）
- `dist/ops-toolbox-v0.3.0-test-{mac,win}-v6.zip` — v6 发布包
