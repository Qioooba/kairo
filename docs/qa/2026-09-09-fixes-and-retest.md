# 2026-09-09 修复与复测记录

范围：最近一周非数据库页面；数据库工作台和原有数据库改动不在本轮范围。真实浏览器测试由 Luna / xhigh 执行，主代理负责修复与环境。

## B1 修复

| 编号 | 修改 | 验证状态 |
|---|---|---|
| R1 | WebService 代码生成所有非静态引擎列表操作统一要求 admin，权限检查先于本地文件与工具访问 | 非 admin 六路径 403 回归通过；真实登录 UI 待台账记录 |
| R2 | SFTP 新建文件改为排他创建；已有同名文件不再被空内容覆盖；真实 SFTP 和 shell 后备均支持 | SFTP 包回归通过；实际远程新建/重名复测待台账记录 |
| R3 | 便笺存在本地未保存草稿时，SSE 不再推进草稿基准 revision | Luna 双页面已复现冲突提示并验证使用最新；保留我的/复制两份仍待补测 |
| R4 | 目录同步计划包含根目录与空子目录，执行时创建目录 | 本地真实临时目录回归通过；浏览器同步复测待台账记录 |
| R5 | 生成文件先完整预检、暂存，再排他发布；失败回滚已发布文件并恢复备份 | 后部同名冲突不留下前部新文件回归通过；UI 落盘复测待台账记录 |
| R6 | 偏好读取先区分不存在与实际读取错误，避免把读取失败当空配置 | 目录冒充偏好文件的读取错误回归通过；页面故障复测待台账记录 |
| R7 | 显式填写无效 JDK 时明确报错，不再自动换用本机其他 JDK | 显式无效 JDK 回归通过；真实工具 UI 待台账记录 |
| R8（B2） | 告警 Webhook 识别 HTTP 200 响应中的非零 errcode，测试发送返回失败，异步失败任务通知进入重试 | notify 包测试通过，包括 200 业务失败后第二次成功；Luna 真实 UI 待复测 |
| R9（B3） | WebService 加载/保存失败时清除缓存，后续读写重试；写盘前检查已有格式，拒绝覆盖损坏或未来版本数据 | webservice 包测试通过，连续读取失败和热缓存遇未来版本均保持原文件；Luna UI 待复测 |

## 已完成的辅助检查

- R10（B4）：修复 JAX-WS/CXF/XFire 生成客户端缺少超时方法及 CXF 调用错误 transport。六引擎实际 JDK6 编译通过，纯 JDK/JAX-WS/CXF 实际 SOAP 调用通过。
- R11（B5）：XFire 使用 WSDLReader 保留 WSDL 基准 URI，解决相对 XSD 导入失败；补齐隔离运行依赖后实际 SOAP 调用通过。
- R12（B6）：同步响应与页面反馈增加目录数量。真实单文件、纯空目录、嵌套空目录与含文件目录同步通过，Luna 核验落盘哈希。
- R13（工作区前端）：深扫采用后端已验证目录状态；逐层展开的空目录清除待验证状态并向上汇总，保留错误、类型冲突和截断限制。远程深扫已复测，逐层展开和回归测试继续进行。
- R13 复测补充：真实 SFTP/FTP 逐层展开后无未验证告警；本地同大小同时间不同内容文件检出；5 项 Node 回归通过。CMP-05 和 CMP-07 已由 Luna 核对原验收要求并记为 PASS。
- R14（工作区前端）：修改路径、选择历史来源或重新配置来源时立即清除旧结果与覆盖入口，取消旧扫描；旧目录展开响应不得写回新来源。语法与目录回归检查通过，真实切换复测进行中。
- R14 复测补充：本地结果勾选后修改路径、切换 SFTP 历史来源，旧结果及覆盖按钮均立即消失，显示重新比较提示。
- R15（工作区前端，P1）：超限/二进制/失败读取后输入文字仍可保存，原先以空版本绕过写入冲突保护。已在 8 MiB + 1 B 专用副本真实复现覆盖。统一所有保存入口要求成功完整读取的版本标记，禁止失败读取后直接覆盖；复测继续进行。
- R15 复测补充：超限和二进制拒绝读取后输入文字，普通及菜单保存均禁用，保护副本哈希不变；10 项 Node 回归通过。8 MiB 文件完整加载及 20,001 行编辑通过；8 MiB 单长行编辑遇内置浏览器控制超时，未完成，不记整组通过。
- B7 复测：将当前 `internal/comparefs/local.go` 与其余工作区代码构建为隔离后端 18117；`go test -mod=mod ./internal/comparefs ./internal/httpserver` 通过。B7 内置浏览器复测 CMP-12 越界 Junction/类型冲突均安全拒绝，CMP-09 取消后重启无旧结果回写。
- B7 WAS 复测：真实 iab 完成应用/批量预检、tar+ZIP 生成、默认非空拒绝、已知产物清理、replace 取消/确认、WAR 抽取修改后打包、输入变化使阶段凭据失效、历史详情/删除等场景，证据见 `tmp/qa-20260909/evidence/real-20260909-b0/ui-waspack.md`。
- WAS 复测发现重复清单原先被后端静默去重；已在 `web/pages/waspack.js` 增加 `duplicateManifestMessage` 前端校验，并让校验失败恢复为明确的配置错误状态。`E:/Tools/nodejs/node.exe --check web/pages/waspack.js` 通过；全量 `web/app.test.js` 仍只剩既有数据库工作台 LOB 格式化失败，本轮不修改数据库代码。

- preferences、wscodegen、sftpclient 包测试通过。
- httpserver 的 CodegenOperations、DirectorySync、Compare、WASPack、WSCodegen 定向测试通过。
- notes.js 语法检查通过。
- upgrade、note、reminder、schedtask、webservice 包测试通过。
- B1 构建通过；实际 Windows 构建脚本以 CGO_ENABLED=0 生成便携 QA 产物；ZIP 内容检查仅含 EXE。
- 以上为辅助检查，不替代内置浏览器真实点击结果。

## 实际测试记录

以 [测试台账](2026-09-09-test-ledger.md) 为准。尚有未执行子项，不能将本记录视为全部验收完成。

隔离夹具及服务说明：`tmp/qa-20260909/ENVIRONMENT.md`。证据：`tmp/qa-20260909/evidence/`。
